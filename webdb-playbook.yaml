---
- name: Webservers setup
  hosts: webservers
  tasks:
  - name: Install httpd
    ansible.builtin.yum:
      name: httpd
      state: persist
      
- name: Dbservers setup
  hosts: dbservers
  tasks:
  - name: Install maria-db
    ansible.builtin.yum:
      name: maria-db
      state: persist


